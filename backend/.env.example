# Copy this file to .env (which is gitignored) and fill in real values.
# Nothing here is loaded unless backend/.env exists.

DEBUG=True

# Generate your own with:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=replace-me-with-a-real-secret-key

# Matches docker-compose.yml's db service. Leave unset to fall back to SQLite.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/flaky